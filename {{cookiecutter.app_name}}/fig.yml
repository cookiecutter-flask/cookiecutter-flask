web:
  build: .
  ports:
    - 8000:8000
  links:
    - db

db:
  image: postgres:9.3
  ports:
    - "5432:5432"
  volumes_from:
   - dbdata

dbdata:
  image: postgres:9.3
  command: true
  volumes:
   - /var/lib/postgresql/data
